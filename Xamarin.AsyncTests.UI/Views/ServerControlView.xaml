<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Xamarin.AsyncTests.UI;assembly=Xamarin.AsyncTests.UI"
    x:Class="Xamarin.AsyncTests.UI.ServerControlView">

    <StackLayout Orientation="Vertical">
        <StackLayout Spacing="10" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
            <Label VerticalOptions="Center" Text="Server:" />
            <Button Text="Connect" Command="{Binding Connect}" />
            <Button Text="Start" Command="{Binding Start}" />
            <Button Text="Disconnect" Command="{Binding Stop}" />
        </StackLayout>
        <StackLayout Spacing="10" Orientation="Horizontal">
            <Switch IsToggled="{Binding UseServer}" />
            <Entry Text="{Binding ServerAddress}" HorizontalOptions="FillAndExpand" />
        </StackLayout>
        <Label Text="{Binding StatusMessage}" HorizontalOptions="FillAndExpand" />

        <StackLayout Spacing="30" Orientation="Horizontal">
            <Switch IsToggled="{Binding AutoStart}" />
            <Label Text="Auto start server" VerticalOptions="Center" />
        </StackLayout>

        <StackLayout Spacing="30" Orientation="Horizontal">
            <Switch IsToggled="{Binding AutoLoad}" />
            <Label Text="Auto load test suite" VerticalOptions="Center" />
        </StackLayout>
    </StackLayout>

</ContentView>


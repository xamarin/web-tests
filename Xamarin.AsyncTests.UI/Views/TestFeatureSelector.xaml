<?xml version="1.0" encoding="UTF-8" ?>
<ContentView VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin.AsyncTests.UI.TestFeatureSelector">

    <StackLayout Spacing="5" Orientation="Vertical" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
        <Label Text="Features:" />
        <Frame>
            <ListView ItemsSource="{Binding Features}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <SwitchCell Text="{Binding Feature.Description}" On="{Binding IsEnabled}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Frame>
        <Label Text="System Features:" />
        <Frame>
            <ListView ItemsSource="{Binding SystemFeatures}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding FullName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Frame>
    </StackLayout>
</ContentView>
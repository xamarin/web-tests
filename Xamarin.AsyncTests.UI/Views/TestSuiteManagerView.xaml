<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Xamarin.AsyncTests.UI.TestSuiteManagerView">

    <StackLayout Orientation="Vertical" >
        <Label Text="{Binding StatusMessage}" HorizontalOptions="FillAndExpand" />
        <StackLayout Spacing="30" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
            <Button Text="Clear" Command="{Binding Stop}" />
            <Button Text="Local" Command="{Binding LoadLocal}" />
            <Button Text="Server" Command="{Binding LoadFromServer}" />
        </StackLayout>
    </StackLayout>

</ContentView>


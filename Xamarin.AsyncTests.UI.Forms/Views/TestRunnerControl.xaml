<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Xamarin.AsyncTests.UI.Forms.TestRunnerControl">

    <StackLayout Spacing="5" Orientation="Vertical">

        <StackLayout Spacing="20" Orientation="Horizontal">
            <Button Text="Run" Command="{Binding Run}" />
            <Button Text="Repeat" Command="{Binding Repeat}" />
            <Button Text="Stop" Command="{Binding Stop}" />
            <Button Text="Clear" Command="{Binding Clear}" />
            <Button Text="Refresh" Command="{Binding Refresh}" />
        </StackLayout>

        <Label Text="{Binding StatusMessage}" HorizontalOptions="FillAndExpand" />

        <Label Text="{Binding CurrentTest}" HorizontalOptions="FillAndExpand" />

    </StackLayout>

</ContentView>

